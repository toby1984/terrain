package de.codesourcery.terrain;

public class MarchingSquares
{

    public void process(Data data,float tileSize,
                        IMeshBuilder meshBuilder,float squareSize)
    {
        float xorigin = -tileSize*data.size/2f;
        float yorigin = -tileSize*data.size/2f;

    }
}
